class Component {
    constructor( name, content ) {
        this.name = name;
        this.content = content;
    }

    setData( content ) {
        this.content = content;
    }

    getData() {
        return this.content;
    }
}